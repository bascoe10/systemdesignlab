# Copy to .env and adjust as needed. Everything has a working default.

# Host ports
GATEWAY_PORT=8080
GRAFANA_PORT=3000
PROMETHEUS_PORT=9090

# BYOA (Bring Your Own Agent) — optional, used by your own tooling.
# The lab never sends data anywhere; these are for your convenience.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
