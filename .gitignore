# Local environment
.env

# Your lab bench — materialized by `sdl start`, disposable by design
workspace/

# sdl state: current level, parked workspaces, your calibrated baseline
.sdl/

# Personal artifacts — never committed
my-journal.md

# Build output
*.test
sdl.exe
